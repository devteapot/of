[package]
name = "match-module"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
hex-core = { path = "../../crates/hex-core" }
log = "0.4"
spacetimedb = "=2.7.1"
worldgen = { path = "../../crates/worldgen" }

[lints.rust]
unsafe_code = "forbid"
