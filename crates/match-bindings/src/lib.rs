//! Generated client bindings for the authoritative match module.
//!
//! Regenerate `module_bindings` with `scripts/generate-bindings.sh` whenever
//! the public SpacetimeDB schema changes.

// The contents of `module_bindings` are owned by SpacetimeDB codegen. Keep
// project-wide strict lints on handwritten crates without patching generated
// output after every schema change.
#![allow(clippy::all, clippy::pedantic)]

pub mod module_bindings;

pub use module_bindings::*;
